# Umgebungsvariablen für die Titanic API
# Kopiere diese Datei zu .env und passe die Werte an

# API-Konfiguration
APP_NAME=Titanic Passagier API
VERSION=1.0.0
DEBUG=false

# Server-Konfiguration
HOST=0.0.0.0
PORT=8000

# Datenbank-Konfiguration (PostgreSQL)
# WICHTIG: Setze ein sicheres Passwort für DB_PASSWORD!
DB_HOST=localhost
DB_PORT=5432
DB_NAME=titanic
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Logging
LOG_LEVEL=INFO

# API-Limits
MAX_PAGE_SIZE=500
DEFAULT_PAGE_SIZE=50

# CORS (für Produktion anpassen)
CORS_ORIGINS=["*"]

# Security (für Produktion anpassen)
ALLOWED_HOSTS=["*"]